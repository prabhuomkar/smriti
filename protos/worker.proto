syntax = "proto3";

option go_package = "worker/";

message GenerateEmbeddingRequest {
  string text = 1;
}

message GenerateEmbeddingResponse {
  repeated float embedding = 1;
}

service Worker {
  rpc GenerateEmbedding(GenerateEmbeddingRequest)
      returns (GenerateEmbeddingResponse) {}
}
